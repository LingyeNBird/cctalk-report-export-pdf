[project]
name = "paper-report-pdf"
version = "0.1.0"
description = "Generate PDF from paper_report.json"
requires-python = ">=3.10"
dependencies = [
  "reportlab>=4.0.0",
]

[tool.uv]
package = false
